[
  {braidnet, [
      {rest_api_token, <<"dummy">>}
  ]},
  {kernel, [
      {logger_level, debug},
      {logger, [
        {handler, default, logger_std_h, #{
          formatter => {logger_formatter, #{
            template => [
              time, " ",
              "[", level, "] ",
              msg,
              "\n"
            ],
            chars_limit => 3_000
          }}
        }}
      ]}
  ]}
].
