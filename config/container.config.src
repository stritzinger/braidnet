[
    {braidnet, [
      {rest_api_token, ${AUTH_TOKEN}}
      {hostname, "braidnet.fly.dev"}
    ]},
    {kernel,[
        {logger_level, ${LOG_LEVEL}},
        {logger, [
          {handler, default, logger_std_h, #{
            formatter => {logger_formatter, #{
              template => [
                time, " ",
                "[", level, "] ",
                msg,
                "\n"
              ],
              chars_limit => 3_000
            }}
          }}
        ]}
    ]}
  ].